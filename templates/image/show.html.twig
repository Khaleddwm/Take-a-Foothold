{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <section>
        <h1 class="text-center">Image</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ image.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ image.name }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ image.description }}</td>
                </tr>
                <tr>
                    <th>Path</th>
                    <td>{{ image.path }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('image_index') }}">Biblithèque</a>

        <a href="{{ path('image_edit', {'id': image.id}) }}">Modifier</a>

        {{ include('image/_delete_form.html.twig') }}
    </section>
{% endblock %}
