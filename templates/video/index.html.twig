{% extends 'base.html.twig' %}

{% block title %}Image index{% endblock %}

{% block body %}
    <section>
        <h1 class="text-center">Videothèque</h1>
        <div class="card-deck">
        {% for video in videos %}
            <div class="col-12 col-lg-4">
                <div class="card">
                    <iframe class="card-img-top" width="560" height="315" src="{{ asset (video.path) }}" alt="Card video cap" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                    <div class="card-body">
                        <h1 class="card-title d-flex justify-content-center fas">{{ video.name }}</h1>
                        <p class="card-text text-justify">{{ video.description }}</p>
                    </div>
                    {{ include('video/_delete_form.html.twig') }}
                </div>
            </div>
        {% else %}
            <h2 class="text-center">Aucune video trouvée</td>
        {% endfor %}
        </div>
        <a href="{{ path('video_new') }}">Ajouter une video</a>
    </section>
{% endblock %}
