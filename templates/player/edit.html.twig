{% extends 'base.html.twig' %}

{% block title %}Edit Player{% endblock %}

{% block body %}
    <section class="container">
        <h1 class="text-center">Modifier un joueur</h1>

        {{ include('player/_form.html.twig', {'button_label': 'Valider'}) }}
        <div>
        {% if player.poster is empty %}
            <a class="text-warning" href="{{ path('player_choice_poster', { 'player' : player.id }) }}">Ajouter une image</a>
        {% else %}
            <a class="text-warning" href="{{ path('player_choice_poster', { 'player' : player.id }) }}">Modifier l'image</a>
        {% endif %}
            </div>
            <a class="text-warning" href="{{ path('player_index') }}">Retour à la liste</a>

        {{ include('player/_delete_form.html.twig') }}
    </section>
{% endblock %}
