{% extends 'base.html.twig' %}

{% block title %}Image index{% endblock %}

{% block body %}

    <h1>Image index</h1>
    <div class="card-deck">
    {% for image in images %}
        <div class="col-xs-12 col-lg-4">
            <div class="card">
                <img class="card-img-top" src="{{ asset ('/uploads/images/' ~ image.path) }}" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">Card title</h5>
                    <p class="card-text">{{ image.description }}</p>
                    <p class="card-text"><small class="text-muted">{{ image.path }}</small></p>
                    <a href="{{ path('player_new_poster', { 'player' : player.id, 'image' : image.id }) }}">Choisir cette image</a>
                </div>
            </div>
        </div>
        <div>
            <a href="{{ path('player_add_poster', { 'player' : player.id}) }}">Ajouter une image</a>
        </div>
    {% else %}
        <h2 class="text-center">no records found</td>
    {% endfor %}
    </div>
    <a href="{{ path('image_new') }}">Create new</a>
{% endblock %}
