{% extends 'base.html.twig' %}

{% block title %}Player index{% endblock %}

{% block body %}
    <section class="">
        <h1 class="text-center">Classement des meilleurs passeurs</h1>

        <table class="table text-white">
            <thead>
                <tr>
                    <th>Nom du joueur</th>
                    <th>Club actuel</th>
                    <th>Passe(s) décisive(s)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for player in classement %}
                <tr>
                    <td>{{ player.name }}</td>
                    <td>{{ player.current_team }}</td>
                    <td>{{ player.total }}</td>
                    <td>
                        <a class="btn btn-outline-warning" href="{{ path('player_show', {'id': player.id}) }}">Fiche</a>
                        {% if app.user %}
                        <a class="btn btn-outline-warning" href="{{ path('player_edit', {'id': player.id}) }}">Modifier</a>
                        {% endif %}
                        <a class="btn btn-outline-warning" href="{{ path('player_stats', {'player': player.id}) }}">Stats du joueur</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="12">Aucun joueur trouvé</td>
                </tr>
            {% endfor %}
                <tr><td></td><td></td><td></td><td></td></tr>
            </tbody>
        </table>

        <a class="text-warning" href="{{ path('index') }}">Retour à l'accueil</a>
    </section>
{% endblock %}

